apiVersion: v1
kind: Pod
metadata:
  name: alpine-pod
  labels:
    app: alpine
    environment: dev
    team: platform
spec:
  containers:
  - name: alpine
    image: alpine:latest
    command: ["sh", "-c", "echo Hello from Alpine && sleep 3600"]
