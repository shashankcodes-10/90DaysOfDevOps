server {
    listen 80;

    location /health {
        default_type text/plain;
        return 200 "healthy\n";
    }

    location / {
        default_type text/plain;
        return 200 "Nginx is running\n";
    }
}
